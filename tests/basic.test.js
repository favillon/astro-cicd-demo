import { describe, it, expect } from 'vitest';

describe('Suma básica', () => {
  it('1 + 1 debe ser 2', () => {
    expect(1 + 1).toBe(2);
  });

  it('2 + 2 debe ser 4', () => {
    expect(2 + 2).toBe(4);
  });
});
